## Development

To get started, clone the repository and install the dependencies:

```bash
git clone https://github.com/champiw/portfolio
cd portfolio

# Install dependencies
nix develop # If you have nix (not required)

npm install 
npm run dev

```
